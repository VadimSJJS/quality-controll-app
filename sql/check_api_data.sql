SELECT ID_NONCONFORMING_PRODUCT, DETECTION_DATE, WEIGHT_TONNES, 
       EQUIPMENT_KEY, 
       DUMP(EQUIPMENT_KEY, 1016) AS dump_equip
FROM NONCONFORMING_PRODUCT 
WHERE ROWNUM <= 5;
/
SELECT DUMP(NOTE, 1016) FROM NONCONFORMING_PRODUCT WHERE ROWNUM <= 3;
/
